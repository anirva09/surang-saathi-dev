# Packet NNNN — <Task Name>

**Status:** READY / BLOCKED / APPROVED FOR PRIVATE
**Base branch:** `rebuild/v1`
**Work branch:** `<feat|fix|chore>/<scope>`

## Objective

One sentence defining the independently testable outcome.

## Read first

- `PROJECT_BRIEF.md` relevant sections
- `docs/DECISIONS.md`
- `docs/ARCHITECTURE.md`
- `docs/UI_PRINCIPLES.md` when UI is affected
- exact existing files named below

## Allowed scope

List exact areas/files the task may create or change.

## Do not touch

List unrelated applications/modules/contracts that must remain unchanged.

## Required contracts

Define exact names, states, types, endpoint shapes, or component semantics that later work depends on.

## UX requirements

Describe field/manager behavior and failure states. Use `Not applicable` only when there is no UI.

## Acceptance criteria

Use objective checks that can fail.

## Required tests

List specific behaviors to protect, not “write tests”.

## Verification

List exact commands expected for this repository state.

## Commit guidance

Suggested commit boundaries/messages when useful.

## Completion report

Use the structure in `docs/ai-studio/MASTER_PROMPT.md`.

## Stop condition

State exactly where implementation stops and what future work must not begin.
